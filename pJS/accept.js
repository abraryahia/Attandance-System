
 function accept(i){
    console.log("sucess")
            for (var i = 0; i < emp.length; i++) {
                emp[i].flage=true
               emp.push()
               console.log(emp[i].flage)
}
}

 function deleteRow(r) {
    var i = r.parentNode.parentNode.rowIndex;
    emp.deleteRow(i);

}